{
  "name": "agentree",
  "version": "0.6.0",
  "private": true,
  "description": "Agentree — všichni AI agenti na jednom místě v reálném čase: Claude, ChatGPT/Codex, Copilot, Gemini, Cursor, Perplexity, Grok, Qwen.",
  "type": "module",
  "bin": {
    "agentree": "bin/agentree.mjs"
  },
  "engines": {
    "node": ">=22.13"
  },
  "files": [
    "bin/",
    "src/",
    "public/",
    "extension/",
    "docs/INSTALL.md",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build:mac": "mkdir -p dist && node scripts/build-macos.mjs",
    "start": "node bin/agentree.mjs",
    "dev": "node --watch-path=src --watch-path=bin bin/agentree.mjs",
    "test": "node --test --test-concurrency=1 \"test/**/*.test.mjs\"",
    "check": "node --check bin/agentree.mjs && node scripts/check-syntax.mjs",
    "pack": "mkdir -p dist && npm pack --pack-destination dist",
    "smoke": "node scripts/smoke-package.mjs",
    "license": "node scripts/license.mjs",
    "qa:desktop": "node scripts/qa-desktop.mjs",
    "qa:keychain": "node scripts/qa-keychain.mjs",
    "fonts:vendor": "node scripts/vendor-fonts.mjs"
  },
  "license": "UNLICENSED"
}
